/// <reference types="cypress" />

describe('Publicação', () => {
    /*it('Criar uma nova publicação', () => {
        //PROCESSO DE LOGIN EM BACKGROUND
        //1.realizar uma requisiçao do tipo POST com email e senha
        //2.capturar o token que é recebido da nossa requisição
        //3.usar a token recebido para salvar no localstorage

        cy.request({
            method: "POST",
            url: `${Cypress.config().apiUrl}users/login`,
            body: {
                user: {
                    email: 'agilizei.rcw@email.com',
                    password: '123456.ABC'
                }
            }

        }).then((loginResponse) => {
            console.log(loginResponse.body)
        })
    });*/
});